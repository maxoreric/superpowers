# Linear + GitHub 完整工作流整合设计

## 概述

将 Linear 作为任务/想法/项目的统一收集池，通过每日规划调度有限的 Claude Code 资源进行开发工作。

## 设计目标

- Linear 作为所有任务的唯一入口
- 每日规划，按优先级/大小/截止日期智能推荐任务
- 最小化修改现有 skills，手术刀式新增功能
- 完全向后兼容（无 Ticket ID 时行为不变）

## 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                        Linear Workspace                      │
├─────────────────────────────────────────────────────────────┤
│  Team: Project-A    Team: Project-B    Team: Side-Projects  │
│  ┌──────────────┐   ┌──────────────┐   ┌──────────────┐     │
│  │ Backlog      │   │ Backlog      │   │ Backlog      │     │
│  │ Todo         │   │ Todo         │   │ Todo         │     │
│  │ In Progress  │   │ In Progress  │   │ In Progress  │     │
│  │ In Review    │   │ In Review    │   │ In Review    │     │
│  │ Done         │   │ Done         │   │ Done         │     │
│  └──────────────┘   └──────────────┘   └──────────────┘     │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    Superpowers Skills                        │
├─────────────────────────────────────────────────────────────┤
│  daily-planning          → 每日规划，推荐任务列表            │
│  picking-a-task          → 从推荐中选择，开始执行            │
│  (existing skills)       → brainstorming, writing-plans...  │
│  finishing-a-dev-branch  → 完成时自动更新 Linear 状态        │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                        GitHub Repos                          │
│         (每个 Team 对应一个或多个 Repo，通过集成关联)         │
└─────────────────────────────────────────────────────────────┘
```

## 变更清单

### 新增 Skills（2个）

| Skill | 职责 | 触发时机 |
|-------|------|---------|
| `daily-planning` | 查询 Linear 跨 Team 待办，按优先级+大小+截止日期排序，推荐今日任务列表 | 每日开始工作时手动调用 |
| `picking-a-task` | 用户选定任务后，获取详情，指导进入 brainstorming 并传递 Issue ID | daily-planning 之后 |

### 修改现有 Skills（2处微调）

| Skill | 修改内容 |
|-------|---------|
| `using-git-worktrees` | 新增 ~5 行：如果有 Ticket ID，分支名必须为 `ID/description` 格式 |
| `finishing-a-development-branch` | 新增 ~8 行：从分支名提取 ID，PR body 自动加 `Fixes ID` |

### 不修改

- `brainstorming` - 不感知 Linear，只接收普通任务描述
- `writing-plans` - 不变
- `executing-plans` - 不变
- 其他所有 skills - 不变

## Skill 详细设计

### `daily-planning`

**触发词：** `/daily-planning`、"今天做什么"、"规划今日任务"

**核心流程：**

1. 调用 Linear API 获取所有 Team 的待办任务
   - 状态：Backlog 或 Todo
   - 已分配给我 或 未分配

2. 按混合策略排序：
   - 优先级权重：Urgent(4) > High(3) > Medium(2) > Low(1)
   - 截止日期权重：过期(+3) > 3天内(+2) > 7天内(+1) > 无(+0)
   - 大小惩罚：S(0) > M(-1) > L(-2)
   - 综合分 = 优先级 + 截止日期 - 大小惩罚

3. 展示 Top 5-10 任务给用户

4. 询问用户选择今日任务

5. 确认后引导到 `/picking-a-task`

**Linear API 调用示例：**

```bash
curl -X POST https://api.linear.app/graphql \
  -H "Authorization: $LINEAR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"query": "{ issues(filter: {state: {type: {in: [\"backlog\", \"unstarted\"]}}}) { nodes { id identifier title priority dueDate estimate team { name } } } }"}'
```

### `picking-a-task`

**触发词：** `/picking-a-task PRJ-123`、"开始任务 PRJ-123"

**核心流程：**

1. 接收 Issue ID（如 PRJ-123）

2. 调用 Linear API 获取任务详情

3. 更新 Linear 状态为 "In Progress"，分配给自己

4. 展示任务摘要

5. 询问下一步：
   - a) 直接开始实现（S 任务推荐）
   - b) 先 brainstorming 讨论设计（M/L 任务推荐）
   - c) 查看更多详情

6. 传递关键信息："分支名必须以 PRJ-123/ 开头"

**与现有工作流的衔接：**

```
picking-a-task
    │
    ├─→ brainstorming (M/L 任务)
    │       └─→ using-git-worktrees (分支名带 ID)
    │               └─→ writing-plans → executing-plans
    │
    └─→ using-git-worktrees (S 任务，直接开始)
            └─→ 实现 → finishing-a-development-branch
                            └─→ PR body 自动带 "Fixes PRJ-123"
```

### `using-git-worktrees` 修改

新增内容（约 5 行）：

```markdown
### Branch Naming with Ticket ID

如果前置 skill（如 picking-a-task）提供了 Ticket ID（如 PRJ-123），
分支名 **必须** 遵循格式：`ID/description`

示例：`PRJ-123/add-oauth-login`

这确保 Linear GitHub 集成能自动关联 PR 到 Issue。
```

### `finishing-a-development-branch` 修改

新增内容（约 8 行）：

```markdown
### Auto-link Linear Issue

创建 PR 前，检查分支名是否包含 Ticket ID：

```bash
branch=$(git branch --show-current)
ticket_id=$(echo "$branch" | grep -oE '^[A-Z]+-[0-9]+' || true)
```

如果提取到 ID，PR body 开头自动添加：
- `Fixes PRJ-123`（Linear 会自动在 PR 合并时关闭 Issue）
```

## 环境配置

### Linear 设置

1. 创建 Linear Workspace
2. 为每个项目创建 Team
3. 配置统一状态流：`Backlog → Todo → In Progress → In Review → Done`
4. 创建统一 Label：Size (`S`, `M`, `L`)、Type (`feature`, `bug`, `idea`, `chore`)
5. 开启 GitHub Integration，关联 Repo 到 Team

### API Key 配置

```bash
# 获取：Linear Settings → API → Create Key
export LINEAR_API_KEY="lin_api_xxxx"
```

### 目录结构变化

```
skills/
├── daily-planning/          # 新增
│   └── SKILL.md
├── picking-a-task/          # 新增
│   └── SKILL.md
├── using-git-worktrees/     # 微调
│   └── SKILL.md
├── finishing-a-development-branch/  # 微调
│   └── SKILL.md
└── ...（其他不变）
```

## 完整工作流示例

```
用户：/daily-planning

Claude：正在查询 Linear 待办任务...

┌────┬───────────┬─────────────────────────┬─────┬──────┬─────────┐
│ #  │ ID        │ Title                   │ Pri │ Size │ Due     │
├────┼───────────┼─────────────────────────┼─────┼──────┼─────────┤
│ 1  │ API-42    │ 修复登录超时 bug        │ 🔴  │ S    │ 今天    │
│ 2  │ WEB-17    │ 添加用户头像上传        │ 🟡  │ M    │ 周三    │
└────┴───────────┴─────────────────────────┴─────┴──────┴─────────┘

选择今日要做的任务：1, 2

---

用户：/picking-a-task API-42

Claude：
┌─────────────────────────────────────┐
│ 📋 API-42: 修复登录超时 bug         │
├─────────────────────────────────────┤
│ 优先级: 🔴 Urgent    大小: S        │
└─────────────────────────────────────┘

✅ 已更新 Linear 状态为 "In Progress"

下一步：a) 直接开始（推荐）  b) 先 brainstorming

用户：a

Claude：创建分支 API-42/fix-login-timeout ...

---

... 开发完成 ...

用户：/superpowers:finish

Claude：检测到分支 API-42/fix-login-timeout
创建 PR，body 包含 "Fixes API-42"
PR 合并后 Linear Issue 自动关闭。
```

## 实现计划

1. 使用 `skill-creator` 创建 `daily-planning` skill
2. 使用 `skill-creator` 创建 `picking-a-task` skill
3. 微调 `using-git-worktrees/SKILL.md`
4. 微调 `finishing-a-development-branch/SKILL.md`
5. 测试完整工作流

## 参考资料

- [Linear GitHub Integration](https://linear.app/docs/github-integration)
- [evangodon/linear-cli](https://github.com/evangodon/linear-cli)
- [AdiKsOnDev/linear-cli](https://github.com/AdiKsOnDev/linear-cli)
